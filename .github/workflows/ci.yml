name: CI - Tests, Lint, and Security

on:
  pull_request:
    branches: [main]

jobs:
  test-lint-build:
    uses: ./.github/workflows/reusable_ci.yml
    with:
      run_build: false
      run_security: true
