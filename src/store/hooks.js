import { useDispatch, useSelector } from "react-redux";

// Typed hooks for TypeScript-like safety (even though we're using JS)
export const useAppDispatch = useDispatch;
export const useAppSelector = useSelector;

